@* @using MarbleWebProject.Helper
@model List<CategoryListModel>

@if(Model.Count>0)
{
    <div class="mb-3 mb-lg-5"></div>
    <div class="pt-2 pb-3">
        <div class="container">
            <div class="row">
                @foreach (var item in Model)
                {
                    <div class="col-sm-6">
                        <div class="banner banner-overlay">
                            <a href="/@item.Url">
                                <img src="@MediaUrlHelper.Build(item.Picture)" alt="Banner">
                            </a>

                            <div class="banner-content banner-content-center">
                                <h3 class="banner-title text-white"><a href="/@item.Url"><strong>@item.Name</strong></h3><!-- End .banner-title -->
                                <a href="/@item.Url" class="btn btn-outline-white banner-link underline">@Html.Translate("#Review#")</a>
                            </div><!-- End .banner-content -->
                        </div><!-- End .banner -->
                    </div>

                    <!-- End .col-sm-6 -->
                }

               
            </div><!-- End .row -->
            <hr class="mt-0 mb-0">
        </div><!-- End .container -->
    </div>
} *@


