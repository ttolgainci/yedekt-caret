@model ProductList
@if (!string.IsNullOrWhiteSpace(Model.StockStatus))
{
    <span class="product-label product-label-stock" style="background-color:@(string.IsNullOrWhiteSpace(Model.StockStatusColor) ? "#a6c76c" : Model.StockStatusColor)">@Model.StockStatus</span>
}
